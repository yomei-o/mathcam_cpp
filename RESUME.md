# RESUME — mathcam_cpp の進捗と残作業

方針は [README.md](README.md)。ここは**現在地・次の一手・実測した知見・落とし穴**を書き続ける
ファイル。引き継ぐ人はこれを上から読めば足りるようにしてある。

## 現在地（2026-08-23）

**写真から高校数学が解ける。** 写真 1 枚 → 記号検出 → 式木 → 答えと手順が通り、ブラウザでも
動く。両言語（C++ / Python）で同じことができ、パリティテストで一字一句縛ってある。

きょう測った数字（すべてこのコミットで再現できる）:

| 物差し | 値 | コマンド |
|---|---|---|
| 実写の教科書 32 問 | **31 / 32**（image0 13/14・image1 12/12・image2 3/3・image3 3/3） | `python tools/real_eval.py --dir test_data` |
| 合成の端から端まで | **297 / 300**（198/200 の回もある） | `mathcam e2e --n 300` |
| 組版 → 解析の往復 | **200 / 200** | `mathcam selftest` |
| ページを 1 枚渡す（囲まない） | 18 / 32 | `python tools/page_eval.py --dir test_data` |
| パリティ 18 スイート | 全部 0 不一致（合計 4500 件超） | `tools/parity/*.py` |

CAS は**高校の教科書に出る形をひととおり**解ける（下の表）。方程式・不等式・因数分解・
三角関数・指数対数・数列と Σ・漸化式・微分積分・極限・面積・複素数解・絶対値・
部分分数分解・円の方程式・ベクトル・場合の数。

検出器は**どちらの言語でも学習できる**（`mathcam train-det` / `tools/train_det.py`）。
これが長いあいだ唯一の非対称だった。GPU でも通してある（次の節）。

### いま残っている穴

* **長い本番の学習をまだ C++ でやっていない**。GPU で 200 step は通したが、出荷モデルを
  置き換えるところまでは回していない（いまの出荷は Ultralytics で作った `sym_det_v5.onnx`）。
* **ページを 1 枚渡す道が 18/32**。囲めば 31/32 なので、切り分け（行と塊）が弱い。
  小学校のページが 0 なのは、答え欄の四角が問題どうしの隙間を埋めて 1 行が 1 塊になるため。
* **確率**（文章題なので式の入口から違う）と**手書き**（CROHME）は未着手。
* 実写で落ちている 1 問は image0 の `(4p + 3)^2` で、p が b に読まれている（字の取り違え）。

### きょう直した「測って初めて見えた」もの

| 症状 | 原因 | 直し方 |
|---|---|---|
| `sin(x)` が `i*n*s*x` | 検出器は 1 字ずつしか出さない。名前に戻す処理が無かった | `pl::fix_fnnames`（並んだ英字が関数名を綴り、右が括弧なら関数呼び出し） |
| `tan(x)` が `a*n*x`、`ln(x)` が `n*x` | l と 1、t と 1 が同じ形。検出器は数字を返す | 名前を綴るときだけ字の候補を複数持つ（1 → l/t、0 → o、5 → s） |
| `x^2y` が `y^(x^2)` | 写真の道はベースラインが無いので箱の下端で代用していた。y は下に突き出るので次の字が上付きに見える | y g p q は箱の高さの 32% ぶん上げる（組版で測った値） |
| `63/20` が `64 × 0` | 2 画素の分数線が確からしさで落ちる | **分数線だけ**しきい値を下げる（0.20 → 0.08）。合成 295→297、実写・ページは変わらず |
| `{1.8×3.5-(10.2-6.8)}×9` の答えが違う | − が 1 本落ちていた（21 記号）／読めた式の欄に畳んだ値が出ていた | しきい値 0.25 → 0.20（実写 30/32 のまま、合成は 148→149/150）／計算問題は書かれたとおりに出す |
| `a - (2x + 1)` が `a - 2*x + 1` | 和の中の和に括弧を付けていなかった | 中置と LaTeX の両方を直し、固定例をパリティに入れた |
| `xy^2` が `(xy)^2` | 多文字の名前を積にしてから ^ を掛けていた | 指数は最後の 1 文字にだけ付ける |
| 実写が 31/32 → 30/32 に落ちた | 「書かれたとおりに出す」ようにしたら `6 ÷ 4/9` と出て、読み戻すと (6÷4)/9 になる | **`÷` の右の分数だけ括弧でくくる**（`×` の右なら (6×4)/9 = 6×(4/9) で同じなので要らない）。31/32 に戻した |

| できたこと | 中身 |
|---|---|
| 両言語 + パリティ | `tools/*.py` が `pure/*.hpp` の鏡。**18 スイート**（expr / arith / calc / solve / seq / factor / curve / limit / area / trig / recur / circle / vector / layout / typeset / dataset / photo / train_det）で**不一致 0**。答えだけでなく**手順の全行**を突き合わせる |
| 式木 | `Num`（厳密有理数）/ `Sym` / `Add`・`Mul`（可変長）/ `Pow` / `Fn` / `Eq`。不変・共有 |
| 正規形 | 同類項をまとめる、同じ底の指数を足す、全順序でソート → `x+1` と `1+x` が同一 |
| 完全冪 | `sqrt(9/4)` → `3/2`、`8^(1/3)` → `2`、`2^(-3)` → `1/8`。閉じないもの（`sqrt(2)`）は残す |
| 展開 | `(x+1)^3` → `x^3 + 3x^2 + 3x + 1`、`(x+2)(x-3)` → `x^2 - x - 6` |
| 構文解析 | 暗黙の掛け算（`5x`, `2(x+1)`）、小数を有理数に（`0.25` → `1/4`）、関数呼び出し |
| 印字 | 中置と LaTeX。**次数の降順**で表示。分数指数は括弧か `sqrt()`（読み直せる形）。係数 -1 は `-x` |
| 根号の簡約 | `sqrt(8)` → `2*sqrt(2)`、`sqrt(18)/3` → `sqrt(2)`。これが無いと `x^2=2` の答えが `1/2*sqrt(8)` になる |
| solve + 手順 | 一次・二次方程式、**一次不等式**、**2 元 1 次の連立方程式**、**連立不等式**。移項・分母を払う・両辺を割る（負の数のときは向きが変わる）・因数分解・積が 0・解の公式・判別式・加減法・代入法・共通範囲。両言語で**出力が一字一句一致**（348 件） |
| 二次不等式 + 連立 | `x^2 - 5x + 6 > 0` → `x < 2 または x > 3`。答えが 1 本の範囲に収まらないので **Solution に「範囲の列」を持たせ**、連立は範囲の列どうしの重なりで解く（境界に `sqrt(2)` が出るので大小は `approx` で比べる） |
| 微分・積分 + 手順 | `pure/calc.hpp` / `tools/calc.py`。多項式・積・商・合成関数・sin cos tan ln exp、定積分 F(b)-F(a)。**先に展開しない**（`(2x+1)^3` → `(2x+1)^4/8`）。できないものは黙って間違えず理由を返す。パリティ 436 件 |
| 数列・Σ + 手順 | `pure/seq.hpp` / `tools/seq.py`。Σ は多項式（k^3 まで）と等比、下端が 1 でなくても解く。項の並びから 等差・等比・階差 を見分けて一般項と和を出す。**答えは展開したままにしない**（有理根定理で `n(n+1)(2n+1)/6` の形に戻す）。パリティ 244 件 |
| 因数分解 | `pure/factor.hpp`。共通因数・たすき掛け・和と差の積・平方の形・因数定理・2 変数の同次 2 次。**有理数の範囲でしか分けない**（x^2 - 2 は分けないと言う） |
| 三角関数・対数 | π を定数に持ち、特別角は厳密な値（sin(pi/6) = 1/2）。log は底つき `log(a, x)`。指数・対数・三角の**方程式**（真数条件・0 <= x < 2pi の範囲つき） |
| 高次方程式・複素数 | 因数定理で 1 次因数に分ける。有理数の解が無ければ n 乗根。判別式が負なら**複素数解**（虚数単位は式木に入れず、答えの文字列で組む） |
| 絶対値 | `|x - 1|` の記法と、絶対値の方程式・不等式（場合分けと「確かめる」つき） |
| 微分の応用 | `pure/curve.hpp`。接線・法線・極値（f'' の符号で判定）・平方完成と頂点 |
| 極限 | `pure/limit.hpp`。代入 / 0-0 の約分 / 最高次で割る / sin x / x |
| 面積 | `pure/area.hpp`。交点で区切って ∫(上 - 下) を絶対値で足す |
| 三角関数の変形 | `pure/trig.hpp`。加法定理・2 倍角（n 倍角）・合成。合成の α は**特別角のときだけ**書き、そうでなければ r までで止める |
| 漸化式 | `pure/recur.hpp`。等差型・等比型・特性方程式・階差型。**出した一般項は必ず検算する**（a_1 から 6 項回して突き合わせ、合わなければ答えを出さない） |
| 部分分数分解 | `cal::apart`。分母が**相異なる 1 次因数の積**のときだけ（Heaviside の隠すやり方）。これで 1/(x^2-1) のような有理関数が積分できる（log の和） |
| 円の方程式 | `pure/circle.hpp`。x と y をそれぞれ平方完成して中心と半径。右辺が 0 なら「1 点」、負なら「図形なし」と言う |
| ベクトル | `pure/vector.hpp`。内積・大きさ・和と差・平行と垂直・なす角。**なす角は cos θ が特別角の値のときだけ**書く（arccos は持ち出さない） |
| 検出器の C++ 学習 | `pure/train_det.hpp`（姉妹リポから移植）。**ONNX をその場で学習する**（頭の 6 本の Conv の手前で止めて損失をつなぐ）。損失は Ultralytics の v8DetectionLoss と一致（`tools/parity/train_det.py`）。GPU は CPU の 3.8 倍（T4、12 step で 63 秒 vs 237 秒）、値は完全一致 |
| 組版 | 式木 → 見た目の木 → 画像 + 記号ごとの正解枠。入れ子（分数の中の根号、根号の中の上付き）まで描ける。**教科書の書き方**（変数はイタリック・マイナスは U+2212）も出せる |
| データセット生成 | `mathcam dataset` / `tools/dataset.py`。**同じ種なら両言語で同じデータ** |
| 記号検出器 | YOLOv8n を合成 20,000 枚で学習（Kaggle T4）。**mAP50 0.9939 / mAP50-95 0.876**。`models/sym_det_v5.onnx`（12.3MB、NMS なし export、232 ノード） |
| レイアウト解析 | 枠 → 式木。合成 **5,600 式で 100%**（`mathcam selftest`） |
| 端から端まで | 検出 → 解析 → solve。**今までの書き方の合成画像で 95〜98%**、**教科書の書き方だと 33.3%**、**実写の教科書は 0 / 12**（下の表を見ること）。CLI は `mathcam photo --img foo.png --crop x0,y0,x1,y1 --steps` |
| WASM デモを公開 | https://yomei-o.github.io/mathcam_cpp/wasm/ 。サンプル画像 5 枚・ファイル選択・カメラ・**式をドラッグで囲む**・向きを 90° ずつ回す・検出枠の重ね描き・手順表示。1 枚 1.3〜2.7 秒。**公開された実物のバイト列**を node に通して s1 が解けることを確認済み |
| 実写の測り方 | `tests/real_photos.txt`（正解表）+ `tools/real_eval.py`。**写真はリポジトリに入れない**（教科書は著作物）。合成の数字だけ見ていると実物で何が起きているか分からない |
| 小学校の計算 | `× ÷` 小数点・帯分数・中括弧を読む。組版は**テキストから直に**（`ts::present_arith`。CAS の木は `0.96 ÷ 1.2` を `4/5` に畳んでしまう）。`selftest --arith` で 300/300。検出器は 39 クラスで学習中 |
| 小学校の手順 | 「かっこの中を計算 → かけ算・わり算 → たし算・ひき算」、帯分数は「仮分数に直す」から。**畳まない木**（`pure/arith.hpp` / `tools/arith.py`）を内側から 1 手ずつ畳む。両言語で手順の全行が一致（`tools/parity/arith.py` 132 件） |

## 数式書体を入れた（2026-08-22 昼）

**学習データの変数は本文用のイタリックで描いていた**（Liberation Italic、Lato Italic）。
実写の教科書は**数式用のイタリック**で、字が違う。指摘を受けて数式書体を探し、
リポジトリに入れた（`fonts/`、3.8MB、ライセンス文つき。詳細は `fonts/README.md`）:

* KaTeX Main + **KaTeX Math Italic**（Web の数式表示で使われているもの。MIT）
* Computer Modern cmr10/cmb10 + **cmmi10/cmmib10**（TeX の数式斜体）
* New Computer Modern Regular/Italic + **NewCMMath**（GUST）
* TeX Gyre Pagella Regular/Italic/**Math**（Palatino 系）、TeX Gyre Heros（サンセリフ）

これで `tools/make_mix.py` の 13 組のうち 9 組が数式書体になり、**どこでも同じ学習データが
作れる**（Kaggle の julia/R のディレクトリに依存しない）。

副産物として穴が 2 つ出た:

1. **OTF（CFF）の枠が Python 側で全部 0**（`glyf` しか読んでいなかった）。TTF では 0 件
   不一致だったので気付けなかった。`ControlBoundsPen` にして両言語一致。
2. **縦棒だけの `1` が学習データに 1 つも無い**。実写の教科書の 1 は旗も台も無い縦棒で、
   手元の書体はどれも旗つき。`ts::Style::plain_one` を足した（**ラベルは 1 のまま、字だけ
   小文字 l のグリフを借りる**。Century Gothic の 1 と同じ形。Century Gothic 自体は
   Windows 専用で再配布できない）。`make_mix.py` は既定 35% で混ぜる。

## 正解表を直した（2026-08-22 夕。**今日の比較はここで測り直した**）

`tools/check_key.py` を書いて切り出しの縁を調べたら、**4 行が字を切っていた**
（`(2x - y)^2` は上付きの 2、`9x^2 - 12x + 4` は行の下端、`49a^2 - 81b^2` は上下の行が混ざり、
`x - 72 + x^2` は上の行が入っていた）。つまり**原理的に読めない行**を「モデルが外した」と
数えていた。直したら当時のモデル（v4）が 25 → **29 / 32（90.6%）**になった。
そのあと教科書体の数字を入れた round 10 で **30 / 32（93.8%）**まで来た（`sym_det_v5.onnx`）。

直した物差しで測り直した値:

| モデル | 実写 32 問 |
|---|---|
| **`models/sym_det_v5.onnx`（round 10 epoch 9、出荷中）** | **30 / 32**（image0 12/14・image1 12/12・**image2 3/3・image3 3/3**） |
| round 6 epoch 3（前に出荷していた v4） | 29 / 32 |
| round 7（data8: 文字の重み + × と文字）epoch 6 | 28 / 32 |
| round 8（data9: 数式書体）epoch 0 / 12 / 15 | 27 / 27 / 26 |
| round 9（data10: 全部 + 縦棒の 1）epoch 0/3/6/9/12/15/18 | 25 / **28** / 27 / 27 / 23 / 26 / 23 |
| **round 10（data11: 教科書体の数字を画像で持つ）epoch 0/3/6/9/12/15/18** | 26 / 25 / 30 / **30** / 30 / 29 / 29 |
| round 11（data12: 教科書体を標準 + 太字の 3 組に）epoch 0/3/9/12 | 25 / 16 / 29 / 30 |

**round 10 が当たった。** 「実物の `1` は旗も台も無い縦棒で、手元のどの書体にも無い」という
一点が、残っていた失敗の 2/3 を占めていた。書体ファイルは配れないので、`mathcam fontdump` で
**必要な字だけ画像にしてリポジトリに入れる**（`fonts/kyokasho`、21 字 74KB）。
epoch 6 も 30/32 だが、小学校のページ（3/3 対 2/3）とページ渡し（19 対 17）で epoch 9 を選んだ。
その後、構造の規則（`drop_unmatched_brackets`）で **31 / 32（96.9%）**になった。

round 11 は教科書体を 3 組（標準 + 太字）にしたが最良 30/32 で、**round 10 を超えなかった**。
教科書体の割合を増やすより、1 組でも入っていることが効いていたということ。
**認識はここで止める**（残る 1 件は `(4p + 3)^2` の `p` が `b` になるもの）。

**round 9 の結論: 縦棒の 1 は的を射たが、合計では v4 を超えなかった。** epoch 0 の重みは
`103 × 12 - 36`（縦棒の 1）を初めて読めた一方で小数点を落としており、epoch 3 では逆に
小数点を拾って縦棒の 1 を落とした。**32 問という物差しでは 1〜4 行の差は揺れの範囲**で、
どのラウンドも 23〜29 に収まる。次に効くのは「学習データの作り方」より
**物差しを増やすこと**（写真を増やす）かもしれない。

残る 3 件はどれも**検出が落ちている**もの（`(5b - a)` の a、`1 2/9` の 1、`103 × 12` の 1）。
うち 2 件が「縦棒だけの 1」なので、round 9（plain_one）が的を射ている。

## ラウンドの記録（実写の正解表で選ぶ。**下の表は直す前の物差しでの値**）

| ラウンド | データ | 変えたところ | 実写の最良 |
|---|---|---|---|
| round 2 | data2 | 教科書の書き方（イタリック + U+2212） | epoch 13 で 19/32 相当（出荷していた `sym_det.onnx`） |
| round 5 | data6 | 書体 13 組・39 クラス（劣化なし） | 8/27（epoch 12）。epoch 18 では 3/27 まで落ちた |
| round 6 | data7 | **写真に近づける劣化**（明暗ムラ・粒子・JPEG） | **25/32**（epoch 3）← いま出荷している `sym_det_v5.onnx` |
| round 7 | data8 | 文字の重みを均す + `× と文字`の同居 | 25/32（epoch 6 と 15）。**epoch 6〜18 が 22〜25 で安定**（round 5/6 は崩れた） |
| round 8 | data9 | **数式書体**（KaTeX Math Italic・cmmi10・NewCM・Pagella・Heros をリポジトリに入れた） | 23 / 21 / 23 / 23（epoch 0/3/6/9）。**数式書体だけでは v4 を超えなかった** |
| round 9 | data10 | 上の全部 + **縦棒だけの `1`**（35%）。data7 の劣化・data8 の文字の重み・data9 の数式書体を合わせたもの | 学習中 |

round 8 で分かったこと: **TeX 系の数式書体を増やしても実写は上がらない**（21〜23 で頭打ち）。
実物の教科書の書体は日本の教科書体で、TeX の書体とは別物なので、字形を「数式っぽく」しても
そこは埋まらない。効いたのは round 6 の**紙の質感**（劣化）で、次に効きそうなのは
round 9 の**縦棒だけの 1**（実写の失敗のうち 3 行がこれ）。

round 7 の内訳（epoch 6）は image0 10/14・image1 12/12・image2 1/3・image3 2/3 で、
**文字の取り違えは減った**（image0 が 9 → 10）が、小学校のページで 1 問落ちて同点だった。

## 対等性の棚卸し（2026-08-22 に実際に走らせて確かめた）

| 機能 | C++ | Python | 縛り（実測） |
|---|---|---|---|
| 式の解析・正規形・印字 | `mathcam eval` | `tools/expr.py` | `parity/expr` 348 件 不一致 0 |
| 解く + 手順 | `mathcam solve` | `tools/solve.py` | `parity/solve` 252 件 不一致 0 |
| 小学校の手順 | `mathcam solve --steps` | `tools/arith.py` | `parity/arith` 212 件 不一致 0 |
| 組版 + 正解枠 | `mathcam render` | `tools/typeset.py` | `parity/typeset` 218 件 不一致 0（往復不変も） |
| データセット生成 | `mathcam dataset` | `tools/dataset.py` | `parity/dataset` 完全一致（枠は 1px も違わない） |
| レイアウト解析 | `mathcam parse` | `tools/parse_layout.py` | `parity/layout` 200 件 不一致 0 |
| 写真 1 枚 端から端まで | `mathcam photo` | `tools/photo.py` | `parity/photo` 20 枚 式・枠とも一致。実写の写真でも手順の全行が一致 |
| 推論ランタイム | 自作（`pure/onnx_run.hpp`） | onnxruntime | 上の photo パリティで縛る（実装を合わせるのが目的ではない） |
| **検出器の学習** | **無い** | `tools/train_det.py`（Ultralytics） | **ここだけ非対称**（下記） |
| ブラウザのデモ | ✅（WASM） | — | 性質上 C++ だけ |

**唯一の穴: 検出器を C++ で学習できない。** `pure/` には姉妹リポから移した学習の道具
（`autograd.hpp` `optim.hpp` `onnx_train.hpp` `trainrt.hpp` `ops2d.hpp`）が全部あるのに、
`mathcam` の CLI に `train` が無い。姉妹リポ（yolov8_cpp / yolo_lpr_cpp）は**同じ道具で
YOLO を C++ で学習している**ので、できないのではなく**まだ配線していない**だけ。
残っている作業として上に足した。

## いま止まっている所（2026-08-22 朝）

**Kaggle のセッションが落ちた**（kbridge が 500 を返す = 遠隔側に届かない）。GPU が要る作業は
新しいセッションと proxy URL を貰うまで進められない。**URL は `scratch/kaggle_base.txt` に置く**
（コミットしない）。

セッションと一緒に消えるもの / 残るもの:

* 消える: `/kaggle/working` の data7 / data8 / runs7（round 6 の重み）。
  **data8 は `python tools/make_mix.py --out /kaggle/working/data8 --exe ./mathcam` で 2 分で作り直せる**
  （そのために混ぜ方をリポジトリに入れた）。
* 残る: 出荷した `models/sym_det_v5.onnx`（round 6 epoch 3）と、手元に落とした候補
  `models/cand_*.onnx`（gitignore なのでリポジトリには入らない）。

GPU が戻ったらやること（この順）:

1. `scratch/kaggle_base.txt` を新しい URL にして kbridge を起こす。
2. リポジトリを clone し直し、`sh build/gcc.sh pure/mathcam.cpp -o mathcam` で建てる。
3. `python tools/make_mix.py --out /kaggle/working/data8 --exe ./mathcam`（36,400 枚 / 2 分）。
   **data8 は文字の重みを均し、`× と文字が同じ絵に出る`形を 8% 入れたもの**（実写で a→x、
   b/p→t、× → x の取り違えが残っているのを狙った）。
4. `python tools/train_det.py --data .../data8/train --val .../data8/val --epochs 18
   --batch 64 --device 0,1 --save-period 3 --project .../runs8 --name sym8`。
5. `python scratch/pick_checkpoint.py --run runs8/sym8 --only epoch0`（3, 6, 9 も）。
   **実写で選ぶ**（合成 val は最後まで 0.99 台で、実写の失敗が見えない）。round 6 は
   epoch 3 が頂点だったので、**早い epoch を先に測る**。
6. 22/27 を超えたら `python scratch/ship_model.py --from models/cand_..._epoch3.onnx
   --name sym_det_v5.onnx` → emcc → node の検査 → 実写 → push。

## 次の一手（2026-08-22 夕の時点。上から順に）

1. ~~認識をもう一段上げる~~ → **31 / 32（96.9%）で止めた（2026-08-22）**。
   残るのは `(4p + 3)^2` の `p` が `b` になる 1 件だけ。ここから先はデータをいじるより
   写真（物差し）を増やすほうが先で、それは手元の 4 枚では作れない。
2. ~~round 9（data10）を測って 29/32 を超えたら出荷~~ → **測った。超えなかった**
   （最良 28/32、epoch 3）。**出荷は `sym_det_v5.onnx` のまま**。
   次にやるなら「データの作り方」より**物差しを増やす**（写真を足す）。いまの 32 問では
   1〜4 行の差が揺れに埋もれ、どの案が効いたのか決められない。
   写真を足したら `tools/check_key.py` で切り出しを検査してから使う。
3. ~~検出器を C++ でも学習できるようにする~~ → **完了**（`mathcam train-det`）。
   次は**長く回して出荷モデルを更新できるか**（いまは小さく回して動くところまで）。
4. **ページ渡しを上げる**（いまは 19/32。囲めば 31/32）。小学校のページが 0 なのは、
   答え欄の四角が問題どうしの隙間を埋めて 1 行が 1 塊になるため。四角を「区切り」として
   扱えるかを測るのが次。却下した案（刻む・谷の下限・横余白）は下の表にある。
5. ~~微分・積分~~ ~~二次不等式・連立不等式~~ ~~数列と Σ~~ ~~因数分解~~ ~~三角関数・対数~~
   ~~高次方程式・複素数~~ ~~絶対値~~ ~~接線・極値~~ ~~極限~~ ~~面積~~ → **全部できた**。
   まだ無い高校の範囲: **直線と円の位置関係**、**確率**（文章題なので入口から違う）、
   分母に重解や 2 次因数がある部分分数、1/(x^2+1) の積分（arctan が無い）。
   手書き（CROHME）も未着手。

## 検出器の C++ 学習を GPU で通した（2026-08-23、Kaggle T4）

`mathcam train-det` は「動く」だけでなく「回して使えるモデルが出る」ところまで確かめてある。
Kaggle の T4 で通した手順と実測:

| 段 | 実測 |
|---|---|
| `sh build/nvcc.sh pure/mathcam.cpp -o mathcam_gpu.exe` | 45 秒（arch sm_75） |
| `train-det --gradcheck` | PASS。**損失の値は Windows と 1 ビット違わない**（99.111366） |
| `selftest` | 200 / 200 |
| `dataset --n 400` | 400 枚 |
| `train-det --steps 200 --batch 8 --cos-lr --val-every 50` | loss 15.40 -> 12.36 |
| 検証（端から端まで、40 件） | @50 **100%** / @100 97.5% / @150 97.5% / @200 97.5% |
| 書き出した best.onnx で `e2e --n 80` | **80 / 80**（出荷中のモデルと同じ） |

**GPU は CPU の 3.8 倍**（同じ Kaggle のマシンで 12 step / batch 4: GPU 63 秒 vs CPU 237 秒）。
損失の値は両方で完全に一致する（7.159093）。device の切り替えは `pure/backend.hpp` の
`bk::gemm_hosted` だけで、計算の中身は同じソースが動く。

**まだやっていないこと**: 長い本番の学習（出荷モデルを C++ の学習で置き換える）。
いまの検証は合成データの端から端までで、実写 32 問で測り直してはいない。

## 済んだこと（記録）

1. ~~**Python 側を書いて、パリティで縛る**~~ → **完了（2026-08-21）**。
   `tools/expr.py` が `pure/expr.hpp` の鏡で、`tools/parity/expr.py` が縛る:
   固定 40 本 + **乱数で作った式**（深さと演算子を振って括弧・暗黙の掛け算・分数指数・
   負数が混ざる）を両実装に通し、**正規形と LaTeX が一字一句同じ**であることと、
   **往復不変**（`print(parse(print(e))) == print(e)`）を確かめる。
   実測: 940 件（固定 40 + 乱数 900）で不一致 0。
   意図的に違うのは数の桁だけ（C++ は int64、Python は多倍長）。
2. ~~**solve と手順表示**~~ → **完了（2026-08-21）**。`pure/solve.hpp` と `tools/solve.py`。
   一次・二次方程式を、名前のついた書き換えの列として解く（移項・分母を払う・両辺を割る・
   因数分解・積が 0・解の公式・判別式）。`tools/parity/solve.py` が
   **CLI の出力（`--steps --latex` の全行）をそのまま突き合わせる**ので、
   表示の文言まで一致を要求する。実測 229 件（固定 29 + 乱数 200）で不一致 0。

   人の書き方に合わせた点: 根が 1/3 なら因数は `(x - 1/3)` ではなく **`(3x - 1)`**
   （分母を因数に吸わせると係数が 1 になる）。「積が 0」の段では**両方の因数**を見せる。
   判別式が平方数でないときは `1/2*(sqrt(5) - 1)` のように**厳密なまま**出す。
3. ~~**数式の組版器**~~ → **完了（2026-08-21）**。`pure/typeset.hpp` + `pure/typeset_impl.hpp`
   と `tools/typeset.py`。式木を**見た目の木**（Row / Sup / Frac / Sqrt / Paren）に直してから
   組版し、画像と**記号ごとの正解枠**を出す。データセット生成は `mathcam dataset` /
   `tools/dataset.py`（YOLO 形式）。

   * **レイアウトはフォント単位の整数で計算**し、画素にするのは最後だけ。実寸は両言語とも
     TTF の hmtx / glyf をそのまま読む（C++ は stb_truetype、Python は fontTools）ので、
     **枠が厳密に一致する**。`tools/parity/typeset.py` が 318 件で不一致 0、
     `tools/parity/dataset.py` が 60 件のデータセット（式・px・全ラベル）で一致。
   * **絵の画素は一致させない**（C++ は stb、Python は FreeType）。一致させるには片方の
     ラスタライザを移植することになり、得られるのは「同じ絵」だけ。パリティは枠に対して取る。
   * ~~フォントはリポジトリに入れない~~ → **入れた（2026-08-22）**。数式書体（KaTeX Math
     Italic・cmmi10・NewCM・TeX Gyre）を `fonts/` に置き、ライセンス文も同梱した。
     実写の変数は数式用のイタリックで、本文用とは字が違う。**どこでも同じ学習データが
     作れる**ことのほうが大事だと判断した（`fonts/README.md`）。
     OTF（CFF）でも両言語で枠が一致する（Python 側は `ControlBoundsPen`）。
4. ~~**記号検出器**~~ → **完了（2026-08-21、Kaggle T4）**。`tools/train_det.py`。
   合成 20,000 枚（`mathcam dataset --n 20000`、外部データセット不要）で YOLOv8n を 40 epoch。
   **mAP50 0.9939 / mAP50-95 0.876**、export は **NMS なし**（自作ランタイムが読める形）。
   クラスは `pure/classes.hpp` の 34 個で、**ここが唯一の正**（学習と推論で番号がずれると
   症状は「たまに変な式になる」だけで原因に辿り着きにくい）。

   数式向けに増強を選び直した: **`fliplr=0.0`（左右反転は禁止）**。`2x=6` を裏返すと
   `x2=6` になり、数式では意味が変わる。回転・せん断は小さく（3 度 / 2 度）、
   `mosaic=0.5` + `close_mosaic=10`。
5. ~~**レイアウト解析**（枠 → 式木）~~ → **完了（2026-08-21）**。`pure/parse_layout.hpp`。
   合成 5,600 式で **100%**（`mathcam selftest`）。設計は
   **外側から構造を潰して原子にする → 平らな列を優先順位で切る**（下の落とし穴の表を参照）。
6. **実写の教科書を読む（いま作業中）**。ユーザーから写真をもらって測ったら **0 / 12**。
   原因は「写真が難しい」ではなく**学習データの書き方が実物と違う**ことだった（実測）:

   | 何を変えたか | 端から端まで |
   |---|---|
   | 今までの書き方（Liberation 立体 + ASCII ハイフン） | 95.0% |
   | 教科書の書き方（イタリック + U+2212 のマイナス） | **33.3%** |

   * U+2212 のマイナスは**検出器が 1 つも取れない**（conf 0.01 まで下げて初めて出る）。
     学習データに '-' しか無いため。長い横棒は分数線に似ているのも効いていそう。
   * イタリックの `x` は Times italic なら読めるが、**教科書の Computer Modern だと `y` に化ける**。
   * 対策: 組版器に第 2 書体（イタリック）と U+2212 を入れ、`dataset` に
     `--italic-pct` / `--minus2212-pct` / `--prefix` を足した。Kaggle 側の書体で
     **cmr10 + cmmi10（TeX の Computer Modern）と STIXGeneral(+Italic) と DejaVu と Liberation**
     を混ぜて 24,000 枚（val 2,400）を作り直し、YOLOv8n を学習中。
   * 幾何の増強（透視・傾き・拡縮）は **Ultralytics に任せる**（`--perspective` などを
     `tools/train_det.py` に出した）。自作の生成器で 2 言語ぶん書くより安く、枠の追随も要らない。
7. ~~**WASM デモ**~~ → **完了・公開済み（2026-08-21）**:
   https://yomei-o.github.io/mathcam_cpp/wasm/ 。`wasm/index.html` + `wasm/worker.js` +
   `wasm/mathcam_wasm.cpp`。サンプル画像はこのリポジトリの組版器で描いた（ライセンス問題なし）。
   `wasm/test_node.js` がブラウザ抜きで同じ道を検査する（PNG 展開器を内蔵、外部依存なし）。
   公開の検査は**Pages から落としたバイト列**（.wasm と .png はローカルと sha256 一致、
   .js は改行だけ違う）を node に通して行った。ブラウザを開けない環境でも
   「公開したものが動く」まで確認できる。
8. **連立方程式**（ユーザーの指示で後回しにした）。必要なもの 3 つ:
   加減法・代入法の手順を出す solver、**複数行のレイアウト**（行の切り分け）、
   `{` のクラス追加（クラス表の末尾に足せば既存モデルと番号は互換）。
9. ~~**Python 側にレイアウト解析と photo を書く**~~ → **完了（2026-08-21）**。
   `tools/parse_layout.py` と `tools/photo.py`。パリティは `tools/parity/layout.py`（枠 -> 式と
   往復）と `tools/parity/photo.py`（同じ画像で式も枠も一致。自作ランタイム vs onnxruntime）。
9b. **小学校の計算**（`× ÷` 小数点・帯分数・中括弧）→ **読む仕組みは完了（2026-08-22）**。
   クラス表の末尾に times / div / dot / brace_l / brace_r を足し、生成器・組版・レイアウト解析・
   dataset・selftest に `--arith` を通した。`selftest --arith` は 300/300。
   **検出器は 39 クラスで学習し直す必要がある**（クラスを足しただけでは検出されない）。
10. **微分**（構造再帰 + 規則表）。ここまで来たら素直。
11. **積分**（表引き + 線形性 + 簡単な置換 + 部分積分）。完全な Risch は狙わない。
12. **手書き**（CROHME）。画像 → LaTeX の系列モデルと、4-5 の検出＋解析を比べる。

## 設計で決めたこと（変えるときは理由を書く）

* 厳密有理数。`double` は表示だけ。
* `Add`/`Mul` は可変長・ソート済み。等価判定は構造の一致。
* 式木は不変（手順表示が過去の段を保持するため）。
* 展開は正規形に入れない（爆発と、くくった形の保持）。
* 正規化は手順に出さない（読める手順にするため）。
* 表示順序（次数の降順）と正規順序（決定的な全順序）は別物。
* 印字は `parse(to_infix(e)) == e` を満たす。
* **写真の道は 1 本だけ**（`pure/pipeline.hpp`）。CLI（`photo` / `e2e`）と WASM が
  同じ関数を通る。前処理を 2 か所に書くと、片方だけ直して「ブラウザだと精度が出ない」になる。
* クラス表は `pure/classes.hpp` 1 か所。追加は**末尾に足すだけ**（番号が動かない）。
* **小学校の書き方は CAS の木から描けない**（`0.96 ÷ 1.2` は正規形で `4/5`、`2 5/8` は `21/8`）。
  絵はテキストから直に作り（`present_arith`）、値は同じテキストを `ex::parse` に通す。
  手順も同じ理由で**畳まない木**（`parse_raw` / `pl::parse_raw`）を通す。
* 畳まない木と通常の木で**構築の道を分けない**（`Parser::bin` / `pl::mk_*` の 1 か所で切り替える）。
  分けると片方だけ直す事故が起きる。

## 落とし穴（実際に踏んだもの）

| 事象 | 症状 | 対策 |
|---|---|---|
| 分数指数をそのまま `^` で印字 | `2^1/2` と出る。これを自分のパーサに戻すと `(2^1)/2` になり、**印字したものを読み直せない** | 分数・負の指数は括弧、`1/2` は `sqrt()` で出す。往復をテストで縛る |
| 正規順序をそのまま表示に使う | `x^2 - 5x + 6` が `6 - 5*x + x^2` と出る。数学としては同じだが読めない | 表示専用の並べ替え（次数の降順）を別に持つ |
| `simp` に展開を入れたくなる | `2(x+1)` を `2x+2` にしたいので入れたくなるが、`(x+1)^100` で爆発し、因数分解の手順表示で「くくった形」が保てない | `expand` を分けて、必要な場所で呼ぶ |
| Bash の heredoc がバックスラッシュを潰す | 改行のエスケープが実改行になり、行末の継続も消える。C++/Python の文字列リテラルを含むパッチが壊れる | パッチは Write/Edit ツールか、`chr(92)` で組み立てる（姉妹リポにも同じ記録がある） |

## 落とし穴（追記。どれもパリティテストが捕まえた）

| 事象 | 症状 | 対策 |
|---|---|---|
| **C++ の評価順が不定** | 乱数を消費する関数を 1 つの式で 2 回呼ぶと、順序は規格上不定。MSVC は右から評価していて、Python 側（左から）と式列が食い違った。つまり「同じ種なら同じデータセット」がコンパイラ依存で崩れていた | 乱数を使う呼び出しは**必ずローカル変数に受けて順序を固定する**。`atom` の分数（分子・分母）でも同じことが起きていた |
| 整数除算の負数 | C++ は 0 方向に切り捨て、Python の `//` は床関数。下付きの負座標で枠が 1px ずれた | Python 側に「0 方向に切り捨てる」`mulr` を書いた。**枠が 1px 違えば学習データとしては別物** |
| 生成器が印刷物に無い書き方を出す | `1x` や `+ -6`。学習データの分布が実物とずれる（教科書は `x`、`- 6` と書く） | 係数 1 は書かない、符号は演算子側に出す。**乱数の呼び方は変えない**（式列が変わってしまう） |

## 落とし穴（認識の側。ここが今回いちばん時間を食った）

| 事象 | 症状 | 対策 |
|---|---|---|
| **正解枠の二重加算** | 組版の `shift()` が枠を動かしているのに、描画側でも足していた。ラベルが**ずれ幅の 2 倍**の位置に出る。絵は正しく見え、**両言語が同じバグなのでパリティも通った** | **描いて読み戻す**検査（`mathcam selftest`）を入れて初めて見つかった。正解データは「もっともらしい」だけでは信用できない |
| レイアウト解析の設計が最初から違った | 分数 → 根号 → ± の順に処理する設計で、入れ子が来ると崩れて **39.3%** で止まった | **外側から潰す**に作り替えた: 分数線と根号の横線のうち**幅の広い方**から原子に潰し、括弧を潰し、平らになった列を優先順位（`=` → `±` → 並置・上付き）で切る |
| 上付きを**大きさ**で判定 | 指数の数字が本文と同じ大きさに見える組版があり、63% で止まった | **ベースラインの差**で判定に変えた（87%）。原子にも `base_y` を持たせ、十分に持ち上がっているときは大きさの条件を緩める |
| 潰した後に並べ替えない | 原子にした後の x 順が壊れて 87% で止まった | 潰すたびに x で並べ直す（99.8%）。最後は根号の**最も近いグリフ**を選ぶ修正で 100% |
| **letterbox を最近傍で拡縮** | 学習（cv2 の bilinear）と別物になり、小さい画像を拡大したとき字がブロック状になる。端から端までが **72.5%** | **双線形**にしたら **98.3%**。前処理は「学習で使ったもの」と同じ関数を通すのが唯一の正解 |
| **NMS がクラスごと** | 同じグリフに `5` と `3` が残って `53` と読む | クラスを無視した重複除去（IoU>0.5）を `pipeline.hpp` に追加 |
| 箱の形が export 依存 | Ultralytics の素の export は **cxcywh**（入力画素）で、スコアは**既に sigmoid 済み** | `BoxFmt::CXCYWH` を明示。姉妹リポにも同じ記録がある |
| パイプで `tail -N` すると走行中のログが空 | 学習の進捗が見えず「動いていない」と誤読する | Ultralytics の `results.csv` を読む（姉妹リポの RESUME にも同じ記録がある。**また踏んだ**） |
| **書き方が実物と違う** | 合成では 95%、教科書の写真では 0/12。字形（イタリック）とマイナスの字（U+2212）が学習データに無いだけだった | 組版器に第 2 書体と U+2212 を入れて作り直す。**「写真が難しい」で終わらせず、描き方を 1 つずつ変えて測る**と原因が出る |
| 1 枚に 1 つの閾値で行を切る（Otsu） | 光沢や影があるページでは暗い側の背景が全部インクになり、帯が 1 つにまとまる | 閾値は**行ごと**に取る（その行の中央値より 30 暗い画素を数える）。それでも行送りが詰まった教科書は割り切れない（4 行が 3 帯） |

## 落とし穴（学習の側。**これがいちばん high 価値な発見**）

| 事象 | 症状 | 対策 |
|---|---|---|
| **合成の val で best を選ぶと実写が悪くなる** | 同じデータで 40 epoch まで回したモデルは、合成 val が mAP50 0.987 / 端から端まで 100% なのに、**実写は 3 / 27**。13 epoch のモデルは実写 18 / 23、epoch 0（合成 40%）でも実写 7 / 27 だった。合成の指標では**この失敗がまったく見えない** | **選ぶ物差しを実写にする**。`--save-period` で途中の重みを残し、`tests/real_photos.txt` で選ぶ（`scratch/pick_checkpoint.py`）。合成データが綺麗すぎて、学習が進むほど「その綺麗さ」に合わせてしまう |
| 横棒の取り違え | 実写で `=` が sqrt 2 個、マイナスが frac や sqrt になる。検出器には**どれも細長い横棒**で区別できない | 見分けるのは**構造**の仕事にした（`pl::fix_bars`）: 同じ x に上下 2 本で間に何も無ければ `=`、上下に中身が無い横棒はマイナス。条件を緩くすると入れ子の分数を壊すので締める |

## 実写のために入れた「構造で直す」規則（どれも実測で 1 件ずつ効いた）

検出器は字の形しか見ない。**同じ形に見えるものは、並びと大きさの比で決める**のが安い。
どれも `pl::` にあり、Python 側にも同じものがある（`tools/parse_layout.py`）。

| 規則 | なぜ | 効果 |
|---|---|---|
| `fix_bars` | マイナス・分数線・根号の上線・`=` の 2 本は全部「細長い横棒」 | `=` が sqrt 2 個になる件 |
| `fix_parens` | `(` `)` と `6` `0` `1` は形が似ている。**括弧は背が高くて細い**（実測 h/med 1.2..1.3、w/h 0.31..0.36。数字は 1.0 と 0.45..0.63）。向きは並びで決める | 実写 18 → 19 |
| 上付きの判定 | 紙が傾くと隣の括弧の塊が 30% 持ち上がって見える。**大きさは土台と比べる**（本文の字と比べると分数指数が落ちる） | `(x+1)(x-2)=0` が `(x+1)^(x-2)=0` になる件 |
| `drop_unmatched_brackets` | 紙の端や印がもう 1 つの括弧として拾われる（実測: `(x - 2)^2 + 3(x - 1)` の後ろに中括弧の右が 1 つ入って解析が落ちた）。**端にあって数が合わない括弧だけ**落とす | 実写 30 → **31 / 32** |
| `strip_dangling` | プリントの答え欄（四角）が `÷` や `+` として検出され、`… = □` の右辺に 1 個だけ残る | 実写 20 → 21 |
| ~~裏写りの下敷きを引いて行を切る~~ | **測って却下。** 実写は裏の印刷が透けていて、**白い行にも 240 画素ほど暗い点がある**（image2）。そのため「最も濃い行の 12%」では帯が 306px（説明文 + 式）にまとまる。移動中央値を引いて山だけ残す案は、窓を小さくすると行の胴まで消え（帯が 20〜50px）、大きくすると下敷きが行自身に持ち上げられる。二段しきい値で広げても、狙った行を全部含む帯は 4 枚中 0〜1 行だけだった（`scratch/band_hp.py`） |
| ~~しきい値を下げる / 桁つなぎの隙間を広げる~~ | **測って却下。** 検出のしきい値は 0.15/0.20/0.25 が同じ 29/32 で、0.30 以上は下がる。0.10 まで下げると縦棒の 1 は見えるが（`l` として）全体は 28/32 に落ちた。桁つなぎの隙間（40%）を 45/50/55% にしても selftest も実写も変わらない | 既定のまま（証拠が無い変更はしない） |
| ~~2 つのモデルを重ねる（アンサンブル）~~ | **測って却下。** 出荷中の v4（25/32）と round 8 epoch 0（23/32）で、行ごとの当たり外れを比べたら「両方 22 / A のみ 3 / B のみ 1 / どちらも外す 6」。**どちらかが当たる行は 26** で、上限でも +1 行。推論 2 倍・モデル 24MB の代償に見合わない（`scratch/model_union.py`） |
| ~~広い塊を刻んで検出~~ | **入れて、測って、取り消した。** 塊が横に長いと 640 に縮む段で字が小さくなるので、幅 6 高さぶんずつ重ねて刻んだ。ところが**刻まずに読めていた行が壊れた**（`{1.8 × 3.5 - (10.2 - 6.8)} × 9` は 1050x100 を 1 回で読めば 26.1 と読める）。ページ渡しは 18/32 → 13/32 | 12 倍まで上げても直らず、取り消した |
| ~~谷で割るときに短い断片を作らない~~ | 同じ実験の一部。説明文と式が 1 帯に入ると式の途中で切るので、断片の下限を入れた。**帯は良くなったが得点は下がった**（image1 8/12 → 6/12） | 取り消した |
| ~~塊の横余白を広く取る~~ | 左端に来た `{` が落ちるので横余白を帯の 1/2 にした。**得点は下がった**（image0 10/14 → 7/14） | 取り消した |
| ~~細い横棒を裏写りとして捨てる~~ | **入れて、測って、取り消した。** 実写では隣の行の下端が 16x3 の `-` として入る。落とそうとして「短くて薄い横棒」を捨てたら、**合成データの本物のマイナスまで落ちて selftest が 100% → 46%**（48px の字でマイナスは 20x4 で、条件に入ってしまう）。実写側の得点は 0 だったので取り消した | 記録だけ残す |
| **OTF（CFF）の枠が Python 側で全部 0** | 数式書体を入れて初めて出た。Python は `glyf` しか読んでいなかったので、CFF の書体では枠が 0 になり C++ と **67/78 食い違った**（TTF では 0 件だったので気付けなかった） | `fontTools` の `ControlBoundsPen` にした（stb_truetype も CFF では制御点の範囲で枠を出す）。TTF/OTF 両方で不一致 0、データセットも完全一致 |
| 数どうしの積に `×` を描く（組版側） | `8 * 6^(3/2)` を並べて描くと `86^(3/2)` と読める（**往復不変が壊れる**） | selftest 2000 件で 100% |

**合成データでは出ない規則ばかり**（末尾の演算子、傾き、答え欄の四角）。だから
`tests/real_photos.txt` を物差しに使う。逆に、これらを入れても合成の 100% が落ちないことを
`selftest --n 2000` と `tools/parity/layout.py`（往復不変も見る）で毎回確かめている。

## 落とし穴（ブラウザの側。**node の検査では見えない**）

| 事象 | 症状 | 対策 |
|---|---|---|
| **重ね描きのカンバスに背景色が付いていた** | `canvas { background: #fff }` を全部の canvas に掛けていたので、上に載せている `#over`（枠を描く透明なはずのカンバス）が**不透明な白**になり、写真が真っ白に見えた。「画像が見えない」「範囲が選べない」の正体はこれ 1 つ | 背景色は `#view` だけに付ける。**人に指摘されるまで気付けなかった**ので、ヘッドレスの Chrome で撮って目で見る道を用意した（`?img=` `?sel=` `?run=`） |
| 写真をそのままの大きさで canvas に描く | 機種によって絵が出ない（iOS Safari は canvas の面積に上限がある）。`getImageData` も落ちる | 表示用に縮めてから描く（長辺 1800 / 面積 3.2M）。渡す絵も 1280 で止める（中で 640 に縮めるので、それ以上送っても精度は上がらない） |
| WASM の `_malloc` の戻りを見ていない | 大きい写真（4000x3000 の RGBA = 48MB）で 0 が返り、そこに書き込んでヒープの先頭を壊す。以後どの操作もでたらめになる | 0 なら「範囲を小さく囲んでください」と返す |
| ドラッグだけで囲ませる | 機種やペンで取りこぼす。「選んだ範囲を読む」が**永久に無効**のまま（実際にそう報告された） | pointer capture を付け、**2 か所クリックでも囲める**ようにする。選択の大きさと位置を数値で画面に出す |
| GitHub Pages の 10 分キャッシュ | 直した版と見ている版が食い違い、症状が噛み合わない | 画面に「ページ更新 <日時>」を出す（`document.lastModified`） |
| node の検査だけで済ませる | `wasm/test_node.js` は WASM の中身は見るが、**表示・選択・重ね描きは 1 行も通らない**。真っ白でも OK が出る | 入口を増やしたら node 側も足す（`mc_run_lines` を追加した）。表示側はヘッドレス Chrome で撮って見る |

## 落とし穴（道具の側。今日はこれで 3 回止まった）

| 事象 | 症状 | 対策 |
|---|---|---|
| `namespace pipe` | **Linux で `pipe does not name a type`**。POSIX の `pipe()` がグローバルにあるため。MSVC では通るので、こちらでは気付けない | `pipeln` に改名。短くてありそうな名前は POSIX と衝突する（`pipe` `index` `link` `time`…） |
| kbridge の `/upload` が先頭の `/` を落とす | `/kaggle/working/...` に送ったつもりが `/kaggle/working/kaggle/working/...` に落ちる。**応答は `ok:true`** なので、そのままビルドして「直したのに直らない」になる | 送り先は**相対パス**で渡す（`mathcam_cpp/pure/x.hpp`）。送った後に `md5sum` か `grep -c` で着いたか確かめる |
| Git Bash が `/kaggle/...` を書き換える | 引数が `C:/Program Files/Git/kaggle/...` になる（MSYS のパス変換）。これも `ok:true` が返る | HTTP を直接叩く（JSON の中身は変換されない）。`MSYS_NO_PATHCONV=1` は**先頭の / を落とす**ので当てにならない |
| Bash の heredoc がバックスラッシュを潰す | `printf("...\n")` を含むパッチを heredoc で流すと、実改行が入って C++ が構文エラー。**今日 3 回踏んだ** | バックスラッシュを含むパッチは Write/Edit ツールで書く。heredoc は「バックスラッシュが 1 文字も無いとき」だけ |
| **/download が古い中身を返す** | 候補の重みを毎回 `models/cand_epoch12.onnx` という同じ名前で落としていたら、round 5 の重みが返ってきて、それを round 6 の値として記録した（実写 8/27。正しくは 20/27）。**サイズまで同じ**なので気付けない | 名前に run を入れる（`cand_runs7_sym7_epoch12.onnx`）。落としたあと **md5 を遠隔と突き合わせる**（`scratch/pick_checkpoint.py` はそうした） |
| 混んでいる Kaggle で 502 | `/job` や `/sh` が 502 Bad Gateway を返す。**セッションは生きている** | 数十秒あけて再試行。ジョブを作る前に一覧を見て、**二重に起動しない**ことを確かめる |

## 環境

* ビルド: `sh build/cc.sh pure/mathcam.cpp -o mathcam.exe`（MSVC、vcvars 不要）/
  `build/gcc.sh`（mingw）/ `sh build/emcc.sh wasm/mathcam_wasm.cpp -o wasm/mathcam.js`（WASM）。
* デモをローカルで見る: `python -m http.server 8000` → `http://127.0.0.1:8000/wasm/`。
  ブラウザ抜きの検査は `node wasm/test_node.js`（`models/sym_det_v5.onnx` を読んで s1.png を解く）。
* `pure/` のエンジン（autograd・ONNX ランタイム・optim・trainrt など）は crowd_cpp から移植済み。
* 学習済みの検出器 `models/sym_det_v5.onnx` は**例外的にリポジトリに入れてある**（デモが動かないと
  意味がないため）。他の `models/*.onnx` は入れない（1 コマンドで作り直せる）。
* GPU が必要になったら姉妹リポの `kaggle_server_cpp`（kbridge）経由。
