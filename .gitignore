*.exe
*.obj
*.o
scratch/*
!scratch/.gitkeep
__pycache__/
*.pyc
.venv/
# 生成した ONNX は入れない（1 コマンドで作り直せる）。学習済みで使えるものだけ例外にする
models/*.onnx
# 事前学習重みとデータセットは各自で用意する
*.pth
*.pt
# 学習済みの検出器はデモが動くように例外的に入れる（他は 1 コマンドで作り直せる）
!models/sym_det_v5.onnx
# 教科書の写真（著作物）は入れない。実写の検証は手元でやる
test_data/
